apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: networks.amplecrd.k8s.io
spec:
  group: samplecrd.k8s.io
  names:
    kind: Network
    plural: networks
  scope: Namespaced
  versions:
    - name: v1
      served: true
      storage: true